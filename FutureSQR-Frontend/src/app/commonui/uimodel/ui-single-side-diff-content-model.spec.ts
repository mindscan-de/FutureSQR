import { UiSingleSideDiffContentModel } from './ui-single-side-diff-content-model';

describe('UiSingleSideDiffContentModel', () => {
  it('should create an instance', () => {
    expect(new UiSingleSideDiffContentModel()).toBeTruthy();
  });
});
