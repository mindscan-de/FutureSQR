import { TransformChangeSet } from './transform-change-set';

describe('TransformChangeSet', () => {
  it('should create an instance', () => {
    expect(new TransformChangeSet()).toBeTruthy();
  });
});
