import { TransformCommitRevision } from './transform-commit-revision';

describe('TransformCommitRevision', () => {
  it('should create an instance', () => {
    expect(new TransformCommitRevision()).toBeTruthy();
  });
});
