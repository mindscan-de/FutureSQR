import { CurrentAuthorizations } from './current-authorizations';

describe('CurrentAuthorizations', () => {
  it('should create an instance', () => {
    expect(new CurrentAuthorizations()).toBeTruthy();
  });
});
