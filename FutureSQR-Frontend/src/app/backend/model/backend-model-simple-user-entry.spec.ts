import { BackendModelSimpleUserEntry } from './backend-model-simple-user-entry';

describe('BackendModelSimpleUserEntry', () => {
  it('should create an instance', () => {
    expect(new BackendModelSimpleUserEntry()).toBeTruthy();
  });
});
