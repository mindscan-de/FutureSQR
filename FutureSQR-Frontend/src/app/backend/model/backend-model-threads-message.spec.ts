import { BackendModelThreadsMessage } from './backend-model-threads-message';

describe('BackendModelThreadsMessage', () => {
  it('should create an instance', () => {
    expect(new BackendModelThreadsMessage()).toBeTruthy();
  });
});
