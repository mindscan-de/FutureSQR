import { BackendModelRevisionFileContent } from './backend-model-revision-file-content';

describe('BackendModelRevisionFileContent', () => {
  it('should create an instance', () => {
    expect(new BackendModelRevisionFileContent()).toBeTruthy();
  });
});
