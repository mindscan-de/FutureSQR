import { BackendModelReviewData } from './backend-model-review-data';

describe('BackendModelReviewData', () => {
  it('should create an instance', () => {
    expect(new BackendModelReviewData()).toBeTruthy();
  });
});
