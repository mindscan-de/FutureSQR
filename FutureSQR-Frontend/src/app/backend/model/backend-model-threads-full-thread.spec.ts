import { BackendModelThreadsFullThread } from './backend-model-threads-full-thread';

describe('BackendModelThreadsFullThread', () => {
  it('should create an instance', () => {
    expect(new BackendModelThreadsFullThread()).toBeTruthy();
  });
});
