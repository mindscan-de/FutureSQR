import { BackendModelCreateReviewResult } from './backend-model-create-review-result';

describe('BackendModelCreateReviewResult', () => {
  it('should create an instance', () => {
    expect(new BackendModelCreateReviewResult()).toBeTruthy();
  });
});
