import { BackendModelReviewResult } from './backend-model-review-result';

describe('BackendModelReviewResult', () => {
  it('should create an instance', () => {
    expect(new BackendModelReviewResult()).toBeTruthy();
  });
});
