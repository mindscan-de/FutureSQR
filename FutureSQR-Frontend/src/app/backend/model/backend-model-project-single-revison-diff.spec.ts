import { BackendModelProjectSingleRevisonDiff } from './backend-model-project-single-revison-diff';

describe('BackendModelProjectSingleRevisonDiff', () => {
  it('should create an instance', () => {
    expect(new BackendModelProjectSingleRevisonDiff()).toBeTruthy();
  });
});
