import { BackendModelSingleCommitFullChangeSet } from './backend-model-single-commit-full-change-set';

describe('BackendModelSingleCommitFullChangeSet', () => {
  it('should create an instance', () => {
    expect(new BackendModelSingleCommitFullChangeSet()).toBeTruthy();
  });
});
