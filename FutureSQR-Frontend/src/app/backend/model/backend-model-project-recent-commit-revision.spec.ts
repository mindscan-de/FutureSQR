import { BackendModelProjectRecentCommitRevision } from './backend-model-project-recent-commit-revision';

describe('BackendModelProjectRecentCommitRevision', () => {
  it('should create an instance', () => {
    expect(new BackendModelProjectRecentCommitRevision()).toBeTruthy();
  });
});
