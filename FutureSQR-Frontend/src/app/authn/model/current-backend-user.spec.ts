import { CurrentBackendUser } from './current-backend-user';

describe('CurrentBackendUser', () => {
  it('should create an instance', () => {
    expect(new CurrentBackendUser()).toBeTruthy();
  });
});
