import { CurrentBackendUserUtils } from './current-backend-user-utils';

describe('CurrentBackendUserUtils', () => {
  it('should create an instance', () => {
    expect(new CurrentBackendUserUtils()).toBeTruthy();
  });
});
