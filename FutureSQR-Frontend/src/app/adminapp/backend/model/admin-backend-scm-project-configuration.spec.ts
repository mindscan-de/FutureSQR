import { AdminBackendScmProjectConfiguration } from './admin-backend-scm-project-configuration';

describe('AdminBackendScmProjectConfiguration', () => {
  it('should create an instance', () => {
    expect(new AdminBackendScmProjectConfiguration()).toBeTruthy();
  });
});
