import { BackendModelProjectItem } from './backend-model-project-item';

describe('BackendModelProjectItem', () => {
  it('should create an instance', () => {
    expect(new BackendModelProjectItem()).toBeTruthy();
  });
});
